networks:
  stream_tools:
    driver: bridge

services:
  rabbitmq:
    image: rabbitmq:4-management-alpine
    networks:
      - stream_tools
    ports:
      - "0.0.0.0:5672:5672" # AMQP
      - "0.0.0.0:15672:15672" # RMQ UI
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
      - ./docker/rabbitmq/10-docker-compat.conf:/etc/rabbitmq/conf.d/10-docker-compat.conf:ro
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  twitch_eventsub:
    build: .
    networks:
      - stream_tools
    depends_on:
      rabbitmq:
        condition: service_healthy
    ports:
      # Explicit host IP so other machines (e.g. a laptop) can reach the Pi over LAN.
      - "0.0.0.0:4343:4343" # OAuth redirect
      - "0.0.0.0:8765:8765" # WebSocket server
    volumes:
      - ./config/docker:/app/config:ro
      - ./tokens.sqlite:/app/tokens.sqlite
      - ./logs:/app/logs
    env_file:
      - .env.compose
    environment:
      TWITCH_DEBUG: "1"
      NO_LOGS: "1"
    stdin_open: true
    tty: true
    command: ["python", "main.py", "--use-rabbitmq", "--use-websockets"]

volumes:
  rabbitmq_data:
